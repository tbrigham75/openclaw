Operational guidelines for `google/gemini-2.5-flash-lite` LLM usage:
- Mindful of request pacing to avoid API rate limits.
- Operating with reduced reasoning/thinking phases to conserve tokens.
- Aware of session length management strategies like `/new` and `/compact` to reduce API costs.